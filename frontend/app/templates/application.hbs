<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
        data-toggle="collapse" data-target="#menu-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class="navbar-brand"}}Home{{/link-to}}
    </div>

    <div class="collapse navbar-collapse" id="menu-collapse">
      <ul class="nav navbar-nav navbar-right">
        {{#if session.isAuthenticated}}
          <li>{{#link-to 'index'}}<i>{{session.currentUser.email}}</i>{{/link-to}}</li>
          {{#if session.currentUser.admin}}
            <li>{{#link-to 'users'}}Users{{/link-to}}</li>
          {{/if}}
          <li><button class="btn btn-link" {{action 'logout'}}>Log out</button></li>
        {{else}}
          <li>{{#link-to 'signup'}}Sign up{{/link-to}}</li>
          <li>{{#link-to 'login'}}Log in{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

<div class="container">
  {{outlet}}
</div>
